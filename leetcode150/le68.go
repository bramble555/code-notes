package leetcode150

import (
	"fmt"
	"strings"
)

func fullJustify(words []string, maxWidth int) []string {
	stringWords := strings.Join(words, " ")
	fmt.Println(stringWords)
	// res := make([]string, 0)
	// count 每次到达 maxWidth 重新开始计数
	// count := 1
	for i := 0; i < len(stringWords); i++ {

	}
	return words
}
